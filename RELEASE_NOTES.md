# XOSS NAV+ ルート転送ツール 0.1.1

リリース日: 2026-08-20

## 概要

GPXの軌跡形状からROの区間境界と左右の区間フラグを推定して生成するように更新しました。従来のテンプレート区間フラグの流用をやめ、終端フラグも出力ルートごとに正しく付与します。従来のGPX変換、ルート分割、USBマスストレージへの追記転送、端末内ルートの削除にも対応しています。

## 検証済み

- 参照GPXの読み込み、距離計算、分割プレビュー
- 距離分割・チェックポイント分割・分割なしのラジオボタン選択
- GPX内のPCと通過チェックをルート順に検出して分割
- 地図プレビューの分割単位ごとの色分け（分割なしは単色）
- プレビュー列幅の縮小と、ウィンドウ位置・サイズ・分割方式の保存
- ROファイルのXZRoutesヘッダーとCRC
- 軌跡形状からの区間フラグ推定
- 最終区間の終端フラグ
- `routebooks.json` とROファイルのサイズ整合性
- 端末内ルートの複数削除（削除時にバックアップを作成しない）
- PyInstallerによるPython不要の単体EXE生成

## 重要な既知の制限

- XOSS NAV+実機への転送と本体でのルート表示は確認済みです。区間フラグ追加後、実機でフリーズしなくなったことが報告されていますが、すべてのルートでの道案内の正確性は未確認です。
- RO形式は公開仕様ではなく、同梱の参照ROファイルをもとに生成しています。
- 地図背景の表示にはネットワーク接続が必要です。取得できない場合は背景なしでルート線を表示します。

詳しい注意事項と操作手順は [README.md](README.md) を参照してください。
